import { StyledText } from "./Instruction.styled";

export const Instruction = () => {
  return (
    <StyledText>
      What are you ready to match with the map? Click on the item from the list,
      then click on the proper area on the map and find out if it's right!
    </StyledText>
  );
};
